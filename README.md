# employee-management-system
Employee Management System is a Java-based console application that manages employee records using OOP concepts. It supports CRUD operations like add, view, search, update, and delete employees using Array List. The project demonstrates modular design with model, service, and main layers.
